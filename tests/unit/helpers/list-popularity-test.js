import { listPopularity } from 'message-board/helpers/list-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | list popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listPopularity([42]);
  assert.ok(result);
});
